# -----------------------------------------------------------------------------
# GIA PHẢ OS - MẪU BIẾN MÔI TRƯỜNG CẤU HÌNH (ENVIRONMENT VARIABLES)
# -----------------------------------------------------------------------------
# Hướng dẫn:
# 1. Chép (copy) file này thành ".env.local"
# 2. Điền các giá trị thực tế tương ứng với PocketBase của bạn

# Tên của ứng dụng web được hiển thị cho người dùng
SITE_NAME="Gia Phả OS"

# URL của PocketBase server (mặc định khi chạy local: http://127.0.0.1:8090)
NEXT_PUBLIC_POCKETBASE_URL="http://127.0.0.1:8090"

# Email đăng nhập của PocketBase superadmin (tạo tại http://127.0.0.1:8090/_/)
POCKETBASE_SUPERADMIN_EMAIL="admin@example.com"

# Mật khẩu của PocketBase superadmin
POCKETBASE_SUPERADMIN_PASSWORD="your-superadmin-password"
